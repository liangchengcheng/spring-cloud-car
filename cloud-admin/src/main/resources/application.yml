spring:
  application:
    name: cloud-admin

server:
  port: ${port:8505}

eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka.serviceUrl:http://127.0.0.1:1111/eureka/}
  instance:
    hostname: ${hostname:admin}
    prefer-ip-address: true
